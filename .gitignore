# Python
__pycache__/
*.py
*.pyc

# Large assets/materials (upload separately if needed)
material/

# Temp/backup files
*backup*
*Full*
*.md

# OS
.DS_Store
Thumbs.db

# Vercel project link (never commit — a stray project.json can turn a local
# `vercel deploy --prod` into an outage across every site on the shared project)
.vercel/
